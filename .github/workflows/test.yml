name: Test
on:
  push:
    branches: [ main ]
  repository_dispatch:
    types: [test]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Test Server
      run: |
        echo "Client payload: ${{ github.event.client_payload }}"